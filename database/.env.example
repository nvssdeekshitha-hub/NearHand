# NEARHAND Database Configuration
# SQLite Default (relative path to database directory)
DATABASE_URL=sqlite:///./nearthand.db

# Example PostgreSQL URL for production migration:
# DATABASE_URL=postgresql://user:password@localhost:5432/nearhand_db

DEFAULT_MAX_CARETAKER_SENIORS=5
ENV=development
